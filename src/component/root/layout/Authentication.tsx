const Authentication = () => {
  return (
    <div>
      <button
        type="button"
        className={`rounded-md bg-teal-600 px-2 py-1 text-xs font-medium text-white transition-colors hover:bg-teal-700 focus-visible:ring-2 focus-visible:ring-teal-400 focus-visible:outline-none sm:px-2.5 sm:py-1.5 sm:text-sm md:px-3 md:py-1.5 lg:px-3.5 lg:py-2`}
      >
        로그인
      </button>
    </div>
  );
};

export default Authentication;
